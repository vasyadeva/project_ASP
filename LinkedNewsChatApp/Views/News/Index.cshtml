@model IEnumerable<LinkedNewsChatApp.Data.News>

@{
    ViewData["Title"] = "Index";
    <head>
    <style>
            html{
                height: auto;
            }
            .column {
                display: flex;
                flex-direction: column;
                margin-bottom: 20px; /* Задати відступ вниз, якщо потрібно */
            }
            
            .news_hd{
                margin-left:45%;
            }

            .article {
                display: flex;
                text-decoration: none;
                color: #000; /* Задати колір тексту, якщо потрібно */
                background-color: #f5f5f5; /* Задати колір фону, якщо потрібно */
                padding: 10px; /* Задати відступи всередині елементу, якщо потрібно */
                border-radius: 5px; /* Задати радіус кутів, якщо потрібно */
            }

                .article:hover {
                    background-color: #e0e0e0; /* Задати колір фону при наведенні, якщо потрібно */
                }

            .article-image {
                flex: 0 0 256px; /* Задати ширину зображення, якщо потрібно */
                margin-right: 10px; /* Задати відступ між зображенням та текстом, якщо потрібно */
            }

            .article-body {
                flex: 1; /* Заповнити залишок доступного простору */
            }

            .article-title {
                font-size: 18px; /* Задати розмір заголовку, якщо потрібно */
                margin: 0; /* Задати відступи заголовку, якщо потрібно */
            }

            .article-content {
                font-size: 14px; /* Задати розмір тексту, якщо потрібно */
                margin-top: 5px; /* Задати відступи тексту, якщо потрібно */
            }

            #article-info {
                position: absolute;
                text-align: center;
                font-size: 12px; 
                margin-top: 10px; 
                color: #777; 
            }

                #article-info span::before {
                    position: absolute;
                    text-align: center;
                    font-size: 12px;
                    margin-top: 10px;
                    color: #777;
                    content: "Опубліковано: "; /* Задати текст перед датою, якщо потрібно */
                }
/*
            a {
                text-decoration: none;
            }
           

            div, h2, p, figure {
                margin: 0;
                padding: 0;
            }

            .main {
                
                margin: 0 auto;
                max-width: 1040px;
                padding: 20px;
            }

           

            .columns {
                display: flex;
                flex: 1;
                flex-direction: column;
            }

            .column {
                display: flex;
                flex: 1;
                flex-direction: column;
            }

            .main-column {
                flex: 3;
            }

            .nested-column {
                flex: 2;
            }

            .article {
                background: white;
                color: #666;
                display: flex;
                flex: 1;
                flex-direction: column;
                flex-basis: auto;
                margin: 10px;
            }

            .article-image {
                background: #eee;
                display: block;
                padding-top: 75%;
                position: relative;
                width: 100%;
            }

                .article-image img {
                    display: block;
                    height: 100%;
                    left: 0;
                    position: absolute;
                    top: 0;
                    width: 100%;
                }

                .article-image img {
                    display: block;
                    height: 100%;
                    left: 0;
                    position: absolute;
                    top: 0;
                    width: 100%;
                }

                .article-image.is-16by9 {
                    padding-top: 56.25%;
                }

            .article-body {
                     display: flex;
                     flex-wrap: wrap;
                     height: 120px;
                     align-content: space-between;
            }

            .article-title {
                color: #333;
                flex-shrink: 0;
                font-size: 1.4em;
                font-weight: bold;
                font-weight: 700;
                line-height: 1.2;
            }

            .article-content {
                flex: 1;
                margin-top: 5px;
            }

            #article-info {
                display: flex;
                font-size: 0.85em;
                justify-content: space-between;
                margin-top: 10px;
            }

            .first-article {
                flex-direction: row;
            }

                .first-article .article-body {
                    flex: 1;
                }

                .first-article .article-image {
                    height: 200px;
                    order: 2;
                    padding-top: 0;
                    width: 300px;
                }
       */
    </style>
    </head>
}

<h1 class="news_hd">Новини</h1>


   <main class="main columns">
@foreach (var item in Model)
{
  <section class="column">
    <a class="article" href="#">
      <figure class="article-image">
                    @{
                        string base64String = Convert.ToBase64String(item.photo);
                    }
                    <img src="data:image/png;base64, @(base64String)" width="256" />
      </figure>
      <div class="article-body">
        <h2 class="article-title">
                        @item.title
        </h2>
        <p class="article-content">
                        @item.text
        </p>
        <footer id="article-info">
                        <span>@item.Time</span>
        </footer>
      </div>
    </a>
  </section>
}

</main>


            